import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color.fromARGB(255, 95, 103, 236);
  static const Color accentColor = Color.fromARGB(255, 225, 225, 0);

  // Text colors
  static const Color textColor = Color.fromARGB(255, 98, 98, 97);

  //Gray
  static const Color grayColor = Color.fromARGB(255, 197, 197, 197);

  // Other colors
  static const Color errorColor = Colors.red;

  // App Bar colors
  static const Color appBarColor = Colors.blueAccent;

  // Button colors
  static const Color buttonColor = Color.fromARGB(225, 251, 144, 87);

  static const Color borderColor = Color.fromARGB(225, 217, 217, 217);
}
